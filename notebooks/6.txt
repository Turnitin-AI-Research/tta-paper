Grade answers

Q:
The comparison <= is used only in Version 1. How would the code from Version 1 be re-written to use only <?

R:
 1: Correct (1.0)
 2: Incorrect. Example: for(j = 1; j < i + 1; j ++) (-0.5)
 3: Did not answer question (0.0)

A:
You can change it to j < i+1.

G: 100
L: 1

A:
    // Version 1 retrieved from:
	// http://mathbits.com/MathBits/Java/arrays/SelectionSort.htm
	// Selection Sort Method for Descending Order
	public static void SelectionSort ( int [ ] num )
	{
	     int i, j, first, temp;  
	     for ( i = num.length - 1; i > 0; i-- )  
	     {
	          first = 0;   //initialize to subscript of first element
	          for(j = 1; j < i+1; j ++)   //locate smallest element between positions 1 and i.
	          {
	               if( num[ j ] < num[ first ] )         
	                 first = j;
	          }
	          temp = num[ first ];   //swap smallest found with element in position i.
	          num[ first ] = num[ i ];
	          num[ i ] = temp; 
	      }           
	}

G: 100
L: 1

A:
The code can be rewritten as below.
public static void SelectionSort ( int [ ] num )
{
int i, j, first, temp;
for ( i = num.length ; i > 1; i-- )
{
first = 0; //initialize to subscript of first element
for(j = 1; j < i; j ++)   //locate smallest element between positions 1 and i.
	          {
	               if( num[ j ] < num[ first ] )         
	                 first = j;
	          }
	          temp = num[ first ];   //swap smallest found with element in position i.
	          num[ first ] = num[ i ];
	          num[ i ] = temp; 
	      }           
	}


G: 110
L: 000

A:
for ( j=0; j < i; i++) 

G: 110
L: 000

A:
change the code from  j <= i to  j< i+1

G: 100
L: 1

A:
for (j = 0; j < i; j++)

G: 110
L: 000

A:
Version 1 can be rewritten to use <= if the rewritten code included the condition "j < i + 1" in the for-loop.

G:
L: